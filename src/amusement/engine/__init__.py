"""Moteurs de règles des jeux, indépendants de tout framework web.

Chaque jeu vit dans son propre sous-package (ex: `orapa_mine`) et expose
une logique pure — modèles de données + règles — testable sans serveur
ni interface graphique.
"""
